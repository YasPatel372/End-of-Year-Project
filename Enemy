import javax.swing.*;

public class Enemy {
    private int x, y, width, height;
    private ImageIcon pic;

    public Enemy(int x, int y, int w, int h) {
        this.x = x;
        this.y = y;
        this.width = w;
        this.height = h;
        pic = new ImageIcon("Boss.png");
        pic = new ImageIcon("ugly.png");
        pic = new ImageIcon("snake.png");
        pic = new ImageIcon("spider.png");
        pic = new ImageIcon("brute.png");
        pic = new ImageIcon("gelatinous.png");
    }

    public int getX() { return x; }
    public int getY() { return y; }
    public int getWidth() { return width; }
    public int getHeight() { return height; }

    public void setX(int x) { this.x = x; }
    public void setY(int y) { this.y = y; }

    public ImageIcon getPic() { return pic; }
}